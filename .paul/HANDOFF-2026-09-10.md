# PAUL Handoff

**Date:** 2026-09-10
**Status:** paused (fim de sessão — init concluído, planejamento não iniciado)

---

## READ THIS FIRST

Você não tem contexto anterior. Este documento diz tudo.

**Project:** sentinela
**Core value:** Um sistema de relato e triagem de focos de arboviroses, que agrega os relatos em áreas de risco e alerta a vigilância municipal.

**O que é:** Sistema de enfrentamento à subnotificação de arboviroses. Cidadãos e agentes de endemias reportam focos (criadouros) pelo mesmo canal; o sistema agrega os relatos em áreas de risco geoespaciais e alerta a vigilância municipal. PWA mobile-first, sem cadastro para o cidadão.

**Contexto acadêmico:** disciplina de 60h, prazo de 3 meses a partir de 2026-09-10. Entregáveis: MVP no ar + artigo + repositório documentado.

---

## Current State

**Version:** 0.0.0
**Phase:** nenhuma definida ainda (roadmap tem apenas placeholder `Phase 1 | TBD`)
**Plan:** nenhum — `.paul/phases/` está vazio
**Milestone:** v0.1 MVP — Relato, Agregação e Dashboard

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Ready for first PLAN]
```

---

## What Was Done

- `/paul:init` executado do zero — `.paul/` criado com PROJECT.md, ROADMAP.md, STATE.md, paul.toml, ledger.toml, config.md, phases/
- Walkthrough de requisitos completo (tipo Application, complexidade inferida `standard`)
- Core value definido e confirmado pelo usuário
- Stack decidida: **Next.js PWA + PostgreSQL/PostGIS + Supabase (Auth/Storage)**, deploy em nuvem gerenciada (Vercel + Supabase)
- 5 core features definidas; **fatia mínima do MVP fixada em features 1 + 3 + 5** (relato do cidadão, agregação em áreas de risco, dashboard de vigilância)
- Modelo de dados de 6 entidades aprovado: Report, RiskArea, Alert, User, Inspection, Municipality
- Constraints levantados (LGPD, offline de campo, custo de storage, auth só para agente/vigilância, CI/CD desde o início)
- 6 success metrics com alvos numéricos definidos
- Integrações configuradas: **SonarQube desabilitado**, **Enterprise Plan Audit HABILITADO**, specialized flows pulado
- BASE v2 não detectado no PATH → `tags = []` em paul.toml, sem integração de knowledge graph

---

## What's In Progress

Nada em execução. Init fechado, nenhum PLAN criado.

**Pendência aberta (assumida, não confirmada explicitamente pelo usuário):** política LGPD default que apliquei ao PROJECT.md —
- coordenada exata visível apenas para agente/vigilância; camada pública usa grid ~100m
- foto em bucket privado com URL assinada
- sem coleta de nome/telefone do cidadão no MVP

O usuário respondeu as outras 4 perguntas daquele bloco mas não contestou nem confirmou essa. Vale revalidar no primeiro `/paul:plan`.

---

## What's Next

**Immediate:** Rodar `/paul:plan` — definir as fases do milestone v0.1 e criar o primeiro plano. O ROADMAP.md ainda tem `Phase 1 | TBD` como placeholder; planejamento precisa preencher a estrutura de fases.

**After that:** `/paul:audit` (Enterprise Plan Audit está habilitado no config.md → o fluxo é `plan → audit → apply → unify`, não `plan → apply → unify`).

**Insumos já prontos para o planejamento (não re-perguntar):**
- MVP = features 1 + 3 + 5; features 2 e 4 são fase 2
- CI/CD entra na fase 1 (decisão registrada)
- Entrega acadêmica (artigo + repositório documentado) deve virar fase própria no roadmap
- Análise de necessidade de integração e-SUS VS / SINAN é item pós-MVP

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Estado vivo do projeto — posição, decisões, deferred issues, blockers |
| `.paul/PROJECT.md` | Requisitos completos: features, modelo de dados, constraints, 8 key decisions, success metrics, tech stack |
| `.paul/ROADMAP.md` | Estrutura de fases — ainda placeholder, preenchido no `/paul:plan` |
| `.paul/config.md` | Enterprise Plan Audit habilitado; SonarQube desabilitado |
| `.paul/paul.toml` | Manifest do projeto (loop position = IDLE) |
| `.paul/phases/` | Vazio — plans entram aqui |

---

## Environment Notes

- **Não é repositório git** (`fatal: not a git repository`). Nenhum commit WIP foi feito. Nenhum código-fonte existe ainda — só `.paul/` e `.claude/`.
- Diretório do projeto: `/home/bidu/tacc/sentinela`
- BASE não instalado — PAUL rodando standalone

---

## Resume Instructions

1. Ler `.paul/STATE.md` para a posição mais recente
2. Ler `.paul/PROJECT.md` — carrega todos os requisitos e decisões do walkthrough
3. Conferir loop position (deve estar em `Ready for first PLAN`)
4. Rodar `/paul:resume` ou `/paul:progress`

---
*Handoff created: 2026-09-10*
